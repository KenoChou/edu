<view class="page-root">
  <ui-page-nav
    top="{{menuTop}}"
    height="{{menuHeight}}"
    title="消息通知"
    background="#f7f8fa"
    bind:back="goBack"
  />

  <view class="main-body">
    <ui-notice-item wx:for="{{notices}}" wx:key="id" notice="{{item}}" />
  </view>
</view>
